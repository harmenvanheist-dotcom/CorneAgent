# Server Configuration
# The port on which the HTTPS application will run
# Default: 3001 (changed from 3000 to avoid conflicts with Open WebUI)
PORT=3001

# HTTP redirect port - HTTP requests here will be redirected to HTTPS
# Default: 8080 (if not set, uses PORT + 8000)
HTTP_PORT=8080

# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
# IMPORTANT: This API key MUST be from the SAME organization and project as your workflow!
OPENAI_API_KEY=your-api-key-here

# ChatKit Workflow ID
# Get this from Agent Builder after clicking "Publish"
CHATKIT_WORKFLOW_ID=your-workflow-id

# Optional: Custom ChatKit API Base URL (leave commented out unless you need it)
# CHATKIT_API_BASE=https://api.openai.com
